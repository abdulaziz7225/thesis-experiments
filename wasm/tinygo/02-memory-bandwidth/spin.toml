spin_manifest_version = 2

[application]
name = "memory-bandwidth-wasm-tinygo"
version = "0.1.0"
description = "memory-bandwidth HTTP service — TinyGo + SpinKube (WASI P1, Wasmtime/Cranelift)"

[[trigger.http]]
route = "/..."
component = "memory-bandwidth"

[component.memory-bandwidth]
# Concurrency for the WASI P1/P2 component is single-threaded per instance;
# horizontal scaling is governed by SpinApp `spec.replicas` in the k8s manifest.
source = "app.wasm"

[component.memory-bandwidth.build]
command = "tinygo build -target=wasip1 -gc=conservative -opt=2 -o app.wasm ."
