module prime-sieve-spin-tinygo

// TinyGo 0.40.1, -target=wasip2.
// No external dependencies: TinyGo's wasip2 runtime exports wasi:http/incoming-handler
// natively via the standard net/http package. No Spin SDK CGo layer needed.
go 1.23
